services:
  - type: web
    name: bowling-league
    env: node
    plan: free
    buildCommand: |
      npm ci
      npm run build
    startCommand: node server.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
