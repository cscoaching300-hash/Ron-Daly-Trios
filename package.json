{
  "name": "bowling-league-serverjs",
  "private": true,
  "version": "1.0.0",
  "description": "Bowling league manager (Express + React) using Lowdb JSON store. Single server.js entry.",
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix client install && npm --prefix client run build",
    "postinstall": "npm --prefix client ci --silent || npm --prefix client install --silent"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "lowdb": "^7.0.1",
"multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=18"
  }
}
{
  "scripts": {
    "build": "cd client && npm ci && npm run build",
    "start": "node server.js"
  }
}
