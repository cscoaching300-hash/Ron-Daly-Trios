{
  "name": "bowling-league-server",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "lowdb": "^7.0.1",
    "multer": "^1.4.5-lts.1",
    "path-to-regexp": "^6.3.0"
  }
}
